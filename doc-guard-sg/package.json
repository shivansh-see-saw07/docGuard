{
  "name": "doc-guard-subgraph",
  "version": "1.0.0",
  "description": "Subgraph for Document Verification System",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ YOUR_USERNAME/doc-guard",
    "create-local": "graph create --node http://localhost:8020/ YOUR_USERNAME/doc-guard",
    "remove-local": "graph remove --node http://localhost:8020/ YOUR_USERNAME/doc-guard",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 YOUR_USERNAME/doc-guard"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.33.0",
    "@graphprotocol/graph-ts": "0.27.0"
  }
} 